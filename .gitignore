node_modules/
# Everything here is runtime/generated data: the live database (and its
# -wal/-shm files), the one-time initial-teacher-credentials file, and any
# local backups/restores created by scripts/backup.bat and restore.bat.
# None of it should ever be committed.
server/data/
server/logs/*.log
server/.env
*.log
.DS_Store
